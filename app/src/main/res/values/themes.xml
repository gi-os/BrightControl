<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.LightControl" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:colorBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <!--
      For an activity that exists to be handed a USB device and then finish. It has no layout and
      lives for less than a frame; without this it would still paint a black window over whatever
      the user was looking at every time they plugged headphones in. See usb/DacAttachActivity.kt.
    -->
    <style name="Theme.LightControl.Invisible" parent="android:Theme.Translucent.NoTitleBar">
        <item name="android:windowAnimationStyle">@null</item>
        <item name="android:windowNoDisplay">true</item>
    </style>

</resources>
